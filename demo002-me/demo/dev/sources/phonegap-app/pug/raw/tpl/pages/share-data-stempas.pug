.view-content
    .text  Deze applicatie vraagt om teostemming voor het gebruik van de volgende:
    .list
        .list-item(ng-repeat="item in $ctrl.items")
            em.mdi.mdi-check.icon
            | {{ item }}
    .form
        label.form-label TOESTAAN VOOR
        .form-group
            select.form-control
                option(value="#") 1 uur
                option(value="#") 6 uur
                option(value="#") 12 uur
                option(value="#") 1 day
                option(value="#") 7 day
                option(value="#") 1 month
                option(value="#") 6 months
                option(value="#") 1 year
                option(value="#") 5 years
                option(value="#") forever
    
    account-switcher

.view-footer: .row
    .col.col-2: a(href="#" ng-click="$ctrl.respond(false)").button.button-primary.button-wide WEIGEREN
    .col.col-2: a(href="#" ng-click="$ctrl.respond(true)").button.button-success.button-wide TOESTAAN