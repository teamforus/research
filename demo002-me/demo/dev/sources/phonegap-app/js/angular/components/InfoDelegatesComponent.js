module.exports = {
    templateUrl: './assets/tpl/pages/info-delegates.html',
    controller: [
        '$state', '$timeout',
        function(
            $state,
            $timeout,
        ) {
            var ctrl = this;
        }
    ]
};