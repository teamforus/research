module.exports = {
    templateUrl: './assets/tpl/pages/info.html',
    controller: [
        '$state', '$timeout',
        function(
            $state,
            $timeout,
        ) {
            var ctrl = this;
        }
    ]
};