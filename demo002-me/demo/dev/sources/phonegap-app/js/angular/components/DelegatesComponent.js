module.exports = {
    templateUrl: './assets/tpl/pages/delegates.html',
    controller: [
        '$state', '$timeout',
        function(
            $state,
            $timeout,
        ) {
            var ctrl = this;
        }
    ]
};